package com.ericsson.hadoop.shell.configuration;

// Not used as of now, will be used if future if required
public class Properties {

	public static final String AVRO_PRINT_HEADER = "avro.printHeader";
}
